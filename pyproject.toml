[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vise-mcp"
version = "0.1.0a20"
description = "Phase-gated workflow enforcer, cross-project experience memory, and git snapshots for Claude Code"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Rixmerz" },
]
keywords = ["mcp", "claude-code", "workflow", "agent", "memory", "snapshots"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "fastmcp>=2.0.0",
    "fastembed>=0.4.0",
    "rich",
    "numpy",
    "pydantic",
    "pyyaml",
]

[project.optional-dependencies]
# The render gates (ui_layout, ui_contrast) drive a real browser: contrast
# against an effective background and per-breakpoint geometry are properties
# of the rendered document, and no stdlib module renders a page. Kept out of
# the core install so `pip install vise` never pulls a browser — every module
# that needs playwright imports it lazily, inside the function that uses it.
design = ["playwright>=1.40"]
dev = [
    "bandit>=1.7.0",
    "coverage>=7.0.0",
    "detect-secrets>=1.5.0",
    # The lsp_clean validator shells out to mypy and `vise doctor` reports it
    # under "vise's own ruff/mypy shell-out" — but it was never installed, so it
    # could only ever resolve to a system mypy that cannot see this project's
    # dependencies and invents missing-stub errors about installed packages.
    "mypy>=1.8.0",
    "types-PyYAML",
    "pip-audit>=2.7.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.6.0",
]

[project.scripts]
vise = "vise.cli.main:main"
vise-mcp = "vise.__main__:run"

[tool.hatch.build.targets.wheel]
packages = ["src/vise"]

[tool.hatch.build.targets.sdist]
include = [
    "src/vise",
    "README.md",
    "pyproject.toml",
]

[tool.coverage.run]
# Here rather than on the CI command line, so the coverage step stays a plain
# `coverage run -m pytest -q` and local runs measure the same thing CI does.
source = ["src/vise"]
omit = ["*/tests/*"]
# Every hook is tested by launching it as its own interpreter — that is the
# only way to test a thing whose contract is "must never take the session
# down". Without `parallel` plus the `sitecustomize` shim on PYTHONPATH, those
# children run uninstrumented and the whole `hooks/` package reports 0%, which
# is the reverse of the truth: it is the best-covered part of the codebase.
# `coverage combine` merges the per-process data files.
parallel = true
concurrency = ["thread", "multiprocessing"]

[tool.coverage.report]
# One floor, in one file. It lived in four — CI said 70, .vise/quality.yaml
# said 71, CLAUDE.md said 74 — so "the coverage gate" meant a different number
# depending on which one you ran, and the one that actually gated was the
# lowest. Every caller now runs a bare `coverage report`.
fail_under = 78
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --strict-markers"
testpaths = ["src/vise/tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src"]

[tool.ruff.lint]
# Pinned, not defaulted. With no `select`, the rule set is whatever the
# installed ruff calls default — and that moves between versions. CI resolved
# `ruff>=0.6.0` to 0.16.1, whose expanded default reported 318 findings against
# a tree that was clean under 0.15.x. A gate whose rules depend on the day you
# ran it is not a gate.
#
# These four are ruff's long-standing default: pyflakes plus the pycodestyle
# errors that catch real breakage. The wider families (BLE001, DTZ, S, PLW, …)
# were evaluated and are ~95% noise on this codebase — 104 of the 318 were
# BLE001 against deliberate fail-open `except Exception` handlers, which is the
# documented contract for every vise hook. Widen this list on purpose, one
# family at a time, never by upgrading the linter.
select = ["E4", "E7", "E9", "F"]
