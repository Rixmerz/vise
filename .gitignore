__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
# `[tool.coverage.run] parallel` writes one data file per process, which is
# what makes a hook launched as its own interpreter measurable.
.coverage.*
.vise/*
!.vise/quality.yaml

# Local session state — active workflow pointer and per-machine permissions.
.claude/workflow/
.claude/settings.local.json
